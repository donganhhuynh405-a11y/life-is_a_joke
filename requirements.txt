# Core dependencies
python-binance>=1.0.19
ccxt>=4.2.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# Database
SQLAlchemy>=2.0.48
psycopg2-binary>=2.9.0

# Web framework (optional)
fastapi>=0.104.0
uvicorn>=0.24.0

# Notifications (optional)
python-telegram-bot>=20.0

# Monitoring (optional)
prometheus-client>=0.19.0

# HTTP
requests>=2.31.0
aiohttp>=3.9.0

# RSS/News feeds
feedparser>=6.0.10

# Security
cryptography>=41.0.0
pydantic>=2.5.0

# System utilities
psutil>=5.9.0

# Lightweight ML / data-science utilities (used in portfolio optimisation and
# basic predictions — small wheels, no CUDA dependencies).
scikit-learn>=1.3.0
joblib>=1.3.0
scipy>=1.11.0
ta>=0.11.0  # Technical analysis indicators

# -----------------------------------------------------------------------------
# Heavy ML packages (PyTorch, TensorFlow, Transformers) are NOT listed here to
# avoid filling the disk of a typical VPS (torch alone pulls ~1.5 GB of CUDA
# libraries when installed from PyPI on Linux).
#
# To enable the advanced ML/AI strategies install the extra requirements:
#   pip install -r requirements-ml.txt
#
# The bot runs fine without them — all heavy-ML code uses lazy imports with
# try/except ImportError so it degrades gracefully.
# -----------------------------------------------------------------------------
